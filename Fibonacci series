#include <stdio.h>

int main(void)
{
    int n;
    if (scanf("%d", &n) != 1) return 0;

    if (n <= 0) return 0;

    long long a = 0, b = 1;
    for (int i = 0; i < n; ++i)
    {
        printf("%lld\n", a);
        long long c = a + b;
        a = b;
        b = c;
    }
    return 0;
}
